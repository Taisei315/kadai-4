<p id="notice"></p>
  <% if book.errors.any? %>
  <h2 class ="red"><%= book.errors.count %> errors prohibited this obj from being saved:</h2>
  <ul class = "red"><% book.errors.full_messages.each do |message| %>
    <li><%= message %></li>
  <% end %></ul>
<% end %>
  <div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <h2>User info</h2>
      <%= attachment_image_tag @user, :profile_image, :fill, 60, 60, fallback: "no_image-72379a4da21cb04a58337cf92d85bc39f8d534cbc78254c2085acaca82236bfa"%>
      <%#= current_user.profile_image_id %>
 <table class="table"><tbody>
  <tr></tr>
  <tr>
	  <th>name</th>
	  <th><%= @user.name  %></th>
  </tr>
  <tr>
	  <th>introduction</th>
	  <th><%= @user.introduction  %></th>
  </tr>
  <%# end %>
</tbody></table>

<div class="row">
  <a class="btn btn-outline-secondary btn-block fas fa-user-cog "
  <%= link_to "", edit_user_path(@user.id) %>
</div></a>